DATABASE_URL=="postgresql://aritrobag@localhost:5432/sevanest?schema=public"
PORT==5100

# Supabase Auth (https://supabase.com/dashboard/project/_/settings/api)
# Email OTP is built-in and free — no SMS provider needed.
SUPABASE_URL==""
SUPABASE_ANON_KEY==""

#Optional (AI + ingestion adapters, gracefully skipped if absent):
GEMINI_API_KEY==        # aiProfileParser, aiSchemeMatcher, aiFollowupService
GROQ_API_KEY==          # aiChatService (Sahayak chat) — https://console.groq.com
GROQ_MODEL==            # optional, defaults to llama-3.3-70b-versatile
MYSCHEME_API_URL==        # mySchemeAdapter
DATA_GOV_API_KEY==        # dataGovAdapter
DATA_GOV_RESOURCE_ID==    # dataGovAdapter